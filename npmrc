; cli configs
long = true
registry = "https://registry.npmjs.org/"

; config
userconfig = "${HOME}/.npmrc"
cache = "${HOME}/.npm"
init-module = "${HOME}/.npm-init.js"
editor = "vim"

email = ""
ignore-scripts = false
loglevel = "http"
message = "%s"
npat = false
onload-script = false

; global
prefix = "/usr/local"
global = true ; If true, All packages go to {prefix}
include = optional
omit = dev

